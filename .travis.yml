language: python
python: 3.6

before_install:
  - sudo pip install pipenv

install:
  - pipenv install --dev

stages:
  - lint
  - test

jobs:
  include:
    - stage: lint

      script:
        - pipenv run lint

    - stage: test

      script:
        - pipenv run tests

notifications:
  email: false
